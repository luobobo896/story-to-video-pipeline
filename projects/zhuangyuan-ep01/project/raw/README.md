# 原始素材目录（只读）

把剧本 txt 直接放进这个目录。

规则：

1. **命名即项目名。** 例如 `雨夜重逢.txt` → 项目名 `雨夜重逢`。文件名无意义时（如 `新建文本文档.txt`），改用剧本首行标题。
2. **这里的文件永不修改。** 任何处理都在 `schema/` 与 `project/work/` 里进行。S0 会记录剧本的 sha256 指纹，原文被改动会立刻暴露。
3. 参考视频、参考图、音乐素材也可以放这里，同样遵守只读原则。
